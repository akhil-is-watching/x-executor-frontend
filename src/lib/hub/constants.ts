/** Hub Nest global prefix — used for direct Hub requests (OAuth, production API base). */
export const HUB_BACKEND_API_PREFIX = "xbot/v1/api";

/** Same-origin path on the frontend dev server; proxied to {@link HUB_BACKEND_API_PREFIX}. */
export const FRONTEND_HUB_API_PREFIX = "api/hub";
